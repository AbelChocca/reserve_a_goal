services:
  db:
    image: postgres:15
    ports:
      - '5432:5432'
    container_name: space_db
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

volumes:
  postgres_data:
